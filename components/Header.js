import Link from 'next/link';

const titleBarStyle = {
  height: '50px',
  width: '100%',
  background: 'green',
  userSelect: 'none',
}

const Header = () => (
  <div style={titleBarStyle}/>
);

export default Header;
